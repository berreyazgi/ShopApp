/** OrderListPage.js — Orders Feature Stub */
export default function OrderListPage() {
  const element = document.createElement('div');
  element.innerHTML = `<div class="container" style="padding-top:4rem;text-align:center"><h1>Siparişlerim</h1><p style="color:var(--color-secondary);margin-top:.5rem">Siparişler modülü geliştirme aşamasında.</p></div>`;
  return { element, destroy: () => {} };
}
